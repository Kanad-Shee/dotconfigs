# DotConfigs

## This is a repo for my necessary configurations for `nvim` and `zsh` shell.

- For `nvim` customization I have used `Lazyvim` as a starter.
- And for `zsh` I have used `zinit` for the plugin manager.
